<services>

  <service name="tar_scm">
    <param name="url">https://github.com/filesender/filesender.git</param>
    <param name="scm">git</param>
    <param name="revision">filesender-2.0-beta2</param>
    <param name="version">2.0</param>
    <param name="exclude">.git</param>
  </service> 

  <service name="recompress">
    <param name="file">*.tar</param>
    <param name="compression">bz2</param>
  </service>

  <service name="download_url">
    <param name="host">raw.githubusercontent.com</param>
    <param name="protocol">https</param>
    <param name="path">/gsfjohnson/build-opensuse-filesender/master/filesender.spec</param>
  </service>

  <service name="download_url">
    <param name="host">raw.githubusercontent.com</param>
    <param name="protocol">https</param>
    <param name="path">/filesender/filesender/master/rpm/filesender.htaccess</param>
  </service>

  <service name="download_url">
    <param name="host">raw.githubusercontent.com</param>
    <param name="protocol">https</param>
    <param name="path">/filesender/filesender/master/rpm/filesender.cron.daily</param>
  </service>

</services>
